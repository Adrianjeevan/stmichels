---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!-- <script>
  gsap.from(".add", {duration: 1, y: 200, ease:'bounce'});
      gsap.from(".section2 ul li", {duration: 1, opacity:0, delay:1, stagger: .1,});
      gsap.from(".figures" , {duration: 1, y:300, opacity:0, delay:1, stagger: .1,})
</script> --><!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Google tag (gtag.js) -->

    <script
      is:inline
      async
      src="https://www.googletagmanager.com/gtag/js?id=G-BW2ZJD6SS2"></script>

    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("js", new Date());

      gtag("config", "G-BW2ZJD6SS2");
    </script>

    <!-- Google Tag Manager -->
    <script is:inline>
      (function (w, d, s, l, i) {
        w[l] = w[l] || [];
        w[l].push({ "gtm.start": new Date().getTime(), event: "gtm.js" });
        var f = d.getElementsByTagName(s)[0],
          j = d.createElement(s),
          dl = l != "dataLayer" ? "&l=" + l : "";
        j.async = true;
        j.src = "https://www.googletagmanager.com/gtm.js?id=" + i + dl;
        f.parentNode.insertBefore(j, f);
      })(window, document, "script", "dataLayer", "GTM-TLX2CWG");
    </script>
    <!-- End Google Tag Manager -->

    <!-- Additionally, paste this code immediately after the opening <body> tag: -->
    <!-- Google Tag Manager (noscript) -->
    <noscript
      ><iframe
        src="https://www.googletagmanager.com/ns.html?id=GTM-TLX2CWG"
        height="0"
        width="0"
        style="display:none;visibility:hidden"></iframe></noscript
    >
    <!-- End Google Tag Manager (noscript) -->

    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image" href="/logo1.png" />
    <meta name="generator" content={Astro.generator} />
    <title> {title}</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ"
      crossorigin="anonymous"
    />

    <!-- slick-- -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css"
      integrity="sha512-tS3S5qG0BlhnQROyJXvNjeEM4UpMXHrQfTGmbQ1gKmelCxlSEBUaxhRBj/EFTzpbP4RVSrpEikbmdJobCvhE3g=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.theme.default.css"
      integrity="sha512-OTcub78R3msOCtY3Tc6FzeDJ8N9qvQn1Ph49ou13xgA9VsH9+LRxoFU6EqLhW4+PKRfU+/HReXmSZXHEkpYoOA=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <!-- slick-- -->

    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
    />

    <!--fancybox-->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/gh/fancyapps/fancybox@3.5.7/dist/jquery.fancybox.min.css"
    />

    <script is:inline src="https://smtpjs.com/v3/smtp.js"></script>
  </head>
  <body>
    <slot />

    <div class="elfsight-app-9a4ba2f6-ab5a-4138-8f79-65b9b32355b5 bg-danger"></div>

    <script
      src="https://static.elfsight.com/platform/platform.js"
      data-use-service-core
      defer></script>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe"
      crossorigin="anonymous"></script>

    <!--carousal-->

    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.3/jquery.min.js"
      integrity="sha512-STof4xm1wgkfm7heWqFJVn58Hm3EtS31XFaagaa8VMReCXAkQnJZ+jEy8PCC/iT18dFy95WcExNHFTqLyp72eQ=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"></script>

    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js"
      integrity="sha512-bPs7Ae6pVvhOSiIcyUClR7/q2OAsRiovw4vAkX+zJbw3ShAeeqezq50RIIcIURq7Oa20rW2n2q+fyXBNcU9lrw=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"></script>
    <!--carousal-->

    <!--fancybox-->
    <script
      src="https://cdn.jsdelivr.net/gh/fancyapps/fancybox@3.5.7/dist/jquery.fancybox.min.js"
    ></script>
    <!--fancybox-->

    <!---GSAP -->

  

    <!---GSAP -->

    <!-- EMAIL -->

    <!-- <script
      is:inline
      src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script> -->

    <!-- EMAIL -->

    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("js", new Date());

      gtag("config", "G-BW2ZJD6SS2");
    </script>

    <script>
      gsap.from(".add", { duration: 1, y: -50, ease: "power1" });
      gsap.from(".media-icons", { y: 50, ease: "power" });

      gsap.from(".banner-txt", { opacity: 0, y: 100 });
      gsap.from(".banner-para", { opacity: 0, y: 100 });

      gsap.from(".round", { y: 300, opacity: 0, stagger: 0.1 });

      gsap.from(".figures", { y: 300, opacity: 0, stagger: 0.3 });

      gsap.from(".wel", {
        scrollTrigger: ".wel", // start the animation when ".box" enters the viewport (once)
        x: 500,
      });

      $(document).ready(function () {
        // setTimeout(() => {
        //   $(".sliders").owlCarousel({
        //     margin: 0,
        //     loop: true,
        //     nav: true,
        //     responsive: {
        //       0: {
        //         items: 1,
        //       },
        //       700: {
        //         items: 2,
        //         margin: 30,
        //       },
        //       1000: {
        //         items: 2,
        //         margin: 30,
        //       },
        //       1200: {
        //         items: 3,
        //         margin: 30,
        //       },
        //     },
        //   });
        // }, 300);

        $(".topper-slider").owlCarousel({
          loop: true,

          nav: true,
          dots: false,
          responsive: {
            0: {
              items: 1,
            },
            320: {
              items: 1,
            },
            400: {
              items: 1,
            },
            600: {
              items: 2,
            },
            1000: {
              items: 3,
            },
          },
        });

        $(".review-slider").owlCarousel({
          loop: true,
          margin: 20,
          nav: true,
          dots: false,
          responsive: {
            0: {
              items: 2,
            },
            400: {
              items: 1,
              margin: 0,
            },
            600: {
              items: 1,
              margin: 0,
            },
            700: {
              items: 2,
              margin: 0,
            },
            1000: {
              items: 2,
            },
            1200: {
              items: 3,
            },
          },
        });
      });
    </script>
  </body>
</html>

<style is:global lang="scss">
  @import url("https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;900&display=swap");
  @import url("https://fonts.googleapis.com/css2?family=Bitter:wght@400;600;700&display=swap");
  :root {
    --body-bg: #5be4f311;
    // --st-color-primary: #f37335;
    --orange: #f37335;
    --lite-yellow: rgba(227, 184, 29, 0.85);
    --orange-gradient: 90deg, rgba(243, 116, 53, 0.845) 0%,
      rgba(242, 121, 51, 0.944) 27%, rgba(240, 128, 48, 0.839) 52%,
      rgba(227, 184, 29, 0.508) 100%;
    --blue-shadow: rgba(48, 177, 241, 0.501);
    --lite-orange: rgba(237, 136, 68, 0.968);
    /* --orange:(237, 136, 68, 0.968); */
    --blue: #135e9e;
    --ash: rgb(113, 119, 125);
    --transition: all 0.3s ease-in-out;
    --body-font: "Montserrat", sans-serif;
    --title-font: "Bitter", serif;
  }

  @media screen and (min-width: 1440px) {
    html {
      font-size: 18px;
    }
  }
  @media screen and (min-width: 1300px) and (max-width: 1440px) {
    html {
      font-size: 16px;
    }
  }
  @media screen and (min-width: 1025px) and (max-width: 1299px) {
    html {
      font-size: 16px;
    }
  }
  @media screen and (min-width: 768px) and (max-width: 1024px) {
    html {
      font-size: 16px;
    }
  }
  @media screen and (min-width: 375px) and (max-width: 425px) {
    html {
      font-size: 15px !important;
    }
  }

  @media screen and (min-width: 650px) and (max-width: 1080px) {
    .title {
      font-size: 30px !important;
      width: 100% !important;
    }

    .course-contents ul li {
      width: 320px !important;
      background-color: red;
    }
  }

  @media screen and (min-width: 400px) and (max-width: 649px) {
    .title {
      font-size: 28px !important;
      width: 100% !important;
      text-align: center;
    }
  }

  /* @media screen and (max-width: 425px) {

  @media screen and (min-width: 650px) and (max-width: 1080px)
 

.title{
font-size: 30px !important;
width: 100% !important;
}
} */

  body {
    font-family: var(--body-font);
    line-height: 1.6;
    // scroll-padding: 75rem;
    background-color: var(--body-bg);
    margin: 0;
    padding: 0;
    overflow-x: hidden;
    // background-color: rgba(195, 226, 250, 0.137);
    /* background: url('../components/images/school-bg.png') center/cover no-repeat ; */

    // /* overflow: hidden; */
  }

  html {
    scroll-padding: 100px;
    scroll-behavior: smooth;
    box-sizing: border-box;
    
    
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  h1, h2, h3, h4, h5, h6, p{
    margin: 0;
  }

  .container {
    // min-width: px !important;
    min-width: 350px !important;
  }
  a {
    text-decoration: none;
    color: black;
  }
  ul li a {
    text-decoration: none;
    color: black;
  }
  .orange {
    color: var(--orange);
  }

  .heading {
    font-weight: 600;
    /* font-size: 80px; */
  }
  .banner-title {
    font-size: 80px;
    color: whitesmoke;
  }

  .banner-txt {
    font-size: 50px;
    font-weight: 900;
    line-height: 1.3;
    letter-spacing: 1px;

    span {
      color: var(--orange);
    }
  }

  .title {
    font-size: 35px;
    font-weight: 900;
    line-height: 1.2;

    /* text-align: center; */
  }

  .title span {
    color: var(--orange);
  }
  .fw600 {
    font-weight: 600;
  }
  .fw900 {
    font-weight: 900;
  }
  .blue {
    color: var(--blue);
  }
  .pb-5 {
    padding-bottom: 5rem;
  }
  .pb-8 {
    padding-bottom: 8rem;
  }

  .ptb {
    padding: 8rem;
  }
  .para {
    text-align: center;
  }

  .blue-bg {
    height: 1100px;
    width: 1100px;
    position: absolute;
    background-color: rgba(111, 189, 248, 0.22);
    top: -230px;
    right: -340px;
    border-radius: 700pc;
    z-index: -33;
  }

  .roundbtn {
    width: auto;
    border: none;
    padding: 0.4rem 1rem;
    // font-size: 18px !important;
    border-radius: 10px;
    border-radius: 25px;
    font-weight: 600;
    border: solid 2px var(--orange);
    background-color: white;

    color: var(--orange);
    transition: var(--transition);

    span {
      font-size: 40px !important;
    }
  }
  .roundbtn:hover {
    background: linear-gradient(
      90deg,
      rgb(243, 115, 53) 0%,
      rgb(242, 120, 51) 27%,
      rgb(240, 128, 48) 52%,
      rgb(227, 183, 29) 100%
    );
    // border: transparent;
    // border: solid 2px var(--orange);
    transition: var(--transition);

    color: white;
    // border:var(--orange);
  }
  .roundbtn a {
    text-decoration: none;
    color: white;
  }
  .roundbtn a:hover {
    color: white;
  }

  .course-contents ul {
    width: 100%;
    height: 100%;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    padding: 0;
    margin: 0;
    align-items: stretch;
    gap: 1rem;
  }

  .course-contents ul li {
    width: 350px;
    height: auto;
    background-color: white;
    /* padding: 1rem; */
    border-radius: 20px;
    // border: 2px solid rgba(93, 160, 242, 0.581);
    // box-shadow: 0px 0px 15px 2px black;
    box-shadow: 0px 0px 0px 3px rgba(7, 7, 7, 0.035);
    transition: var(--transition);
    position: relative;
    text-align: center;
    display: flex;
    justify-content: center;
    padding: 1rem 2rem;
    gap: 1rem;
    align-items: center;
    flex-direction: column;
    font-size: 16px;
  }

  .course-contents ul li .heading {
    height: 20%;
    font-size: 24px;
    padding-bottom: 2rem;
    color: var(--blue);
  }

  .course-contents ul li .pencil {
    position: absolute;
    height: 40px;
    right: 20px;
    bottom: 20px;
    overflow: hidden;
    transition: var(--transition);
  }

  .course-contents ul li .pencil img {
    height: 100%;
    widows: 100%;
    opacity: 30%;
    object-fit: contain;
    transition: var(--transition);
  }

  .course-contents ul li:hover {
    box-shadow: 10px 10px 15px 2px rgba(167, 194, 237, 0.372);
  }
  .course-contents ul li:hover {
    transform: scale(1.02);
    // background-color: rgb(242, 175, 40);
  }

  .course-contents ul li:hover .pencil {
    transform: scale(1.9);
    right: 50px;
    bottom: 50px;
  }

  .course-contents ul li p {
    height: 60%;
  }

  *:focus {
    outline: none;
  }

  // @media screen and (max-width: 768px) {
  //   .course-contents{
  //     ul{
  //       li{
  //         width: 250px !important;
  //       }
  //     }

  //   }
  // }

  @media screen and (max-width: 425px) {
    .sub-title {
      font-size: 40px !important;
    }
    .words {
      font-size: 30px !important;
      width: 90% !important;
    }
  }
  .active-nav {
    background-color: var(--body-bg);
  }

  /*# sourceMappingURL=global.css.map */
</style>

<script>
  initTilt("body", ".color-accent-motion");
  let tiltSettings = [];
  function initTilt(wrapper, elements) {
    const $wrapper = $(wrapper);
    const $elements = $(elements);
    if ($wrapper.length < 1 || $elements.length < 1) {
      return;
    }

    /** Init TweenMax  */
    TweenMax.set([$elements], { transformStyle: "preserve-3d" });

    /** Add event listeners fot tilt effect */
    $wrapper.mousemove((e) => {
      tilt(e.pageX, e.pageY, $wrapper, $elements);
    });

    /** Add event listeners fot mouse left */
    $wrapper.mouseleave((e) => {
      tilt($wrapper.width() / 2, $wrapper.height() / 2, $wrapper, $elements);
    });
  }

  /**
   * Shelly tilt
   *
   * @param cx
   * @param cy
   * @param $wrapper
   * @param $elements
   */
  function tilt(cx, cy, $wrapper, $elements) {
    const sxPos = ((cx / $wrapper.width()) * 100 - 50) * 2;
    const syPos = ((cy / $wrapper.height()) * 100 - 50) * 2;
    let elementsCounter = 0;

    /** Separate animation for each element */
    for (const $element of $elements) {
      /** Set random tilt multiplier */
      if (tiltSettings[elementsCounter] === undefined) {
        tiltSettings[elementsCounter] = Math.floor(Math.random() * 25) * 0.01;
      }

      /** Apply effect */
      TweenMax.to($element, 2, {
        rotationY: -tiltSettings[elementsCounter] * sxPos,
        rotationX: tiltSettings[elementsCounter] * syPos,
        transformPerspective: 500,
        transformOrigin: "center center 200",
        ease: Expo.easeOut,
      });

      elementsCounter++;
    }
  }
</script>
